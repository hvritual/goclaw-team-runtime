syntax = "proto3";

package workspace.v1;

option go_package = "github.com/multica-ai/multica/server/gen/go/workspace/v1;workspacev1";

// SettingService owns configuration whose meaning and lifecycle belong to one
// Workspace. User preferences and System upgrade policy are outside its scope.
service SettingService {}
