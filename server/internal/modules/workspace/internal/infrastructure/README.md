# Workspace infrastructure

Place database, cache, messaging, and external-service adapters here. Adapters implement ports declared by domain or application packages and are private to this module. Select a database explicitly with dddgen persistence; the module command does not choose one.
