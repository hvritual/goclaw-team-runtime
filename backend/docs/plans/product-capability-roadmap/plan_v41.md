# Product capability roadmap v41 — Release 3 aggregate review-block successor

- Plan-ID: `PRODUCT-CAPABILITY-ROADMAP-001`
- Plan-Version: `41`
- Task-Revision: `r046`
- Work-Item: `PCR-RELEASE-3-DONEGATE`
- Exact base: `a63bf58a0098e5b39bd04f0d96efcbc97e9f6a9f`
- Release 3 base: `80d92b14b1f5a1525fbce0c60ce992e28c7f0e8b`
- Predecessor plan: `plan_v40.md`
- Predecessor plan hash: `1ee924cf9170866ba312f9b71b20e081008541dbe935f0fb56351fef2f71f3a7`
- Status: `approved-active`
- Authority: the Human Customer's confirmed continuous Release 3 completion
  direction, repeated confirmed execution, and confirmed prerequisite minimal
  outline authority

## Successor trigger

Exact r045 candidate `a63bf58a0098e5b39bd04f0d96efcbc97e9f6a9f`
has tree `aba1d1c166c9a5667d24f837c0bf0236b3df5b5e` and r045 binary
patch SHA-256
`f9322809dba511553c127c490705c6438943ad6466c631ff504ae2bc23b9260b`.
It changes exactly the six v40-authorized roadmap documents, changes no product
byte, has zero `server/**`, generated, or original-dirty overlap, and retains
closed owned processes and ports. Fresh independent review returns
`CODE/SECURITY/QUALITY PASS` but `SPEC BLOCK`, so r045 stops before R45.6 and
cannot close Release 3.

The review identifies two exact governance failures:

1. Commit `9fb86ea056d253c82ea61b13031550db82eeb526` contains all nine
   registered v27/r032 fields and values in their frozen order, but one blank
   line separates every field. `git interpret-trailers --parse` therefore sees
   only the final `Policy-Bundle`; the r045 audit's `1 x 8 + 45 x 9 continuous`
   conclusion is false. v27 and the governing backend policy required the
   registered fields to be present but did not freeze a continuous-block shape.
   The historical commit remains immutable.
2. Inherited v39 R44.4 required removal of every external evidence directory.
   Two exact, pre-validated PowerShell removal requests were rejected by host
   policy before execution. The task-owned external root
   `F:\codex-tmp\release3-r45-gates-5fad0615` therefore remains with 11,836
   files, 527 directories, and 1,101,286,336 bytes at successor discovery;
   nested `acceptance` accounts for 2,846 files, 265 directories, and
   353,626,627 bytes. It owns no process or listener, but deletion did not
   occur and r045 had no authority to waive it.

v41 corrects those two predicates prospectively. It does not amend v27, v39,
v40, r045 evidence, the independent BLOCK, or any product/story contract.

## Immutable inheritance and exact writable boundary

The four reviewed S07A-D candidate/tree/closure tuples, capability-matrix
reconciliation, R45.3 deterministic evidence, R45.4 installed behavior,
retained NON-PASS disclosures, product-byte boundary, and every v39/v40
exclusion remain binding. Because r046 changes governance only on the exact
unchanged product tree, it must verify rather than rerun those product/runtime
gates.

r046 may change only these five roadmap paths:

- `backend/docs/plans/product-capability-roadmap/plan_v41.md` at R46.1 only;
- `backend/docs/plans/product-capability-roadmap/plan.md`;
- `backend/docs/plans/product-capability-roadmap/task-register.md`;
- `backend/docs/plans/product-capability-roadmap/story-map.md`; and
- `backend/docs/plans/product-capability-roadmap/journal.md`.

`plan_v41.md` is immutable after activation. `capability-matrix.md`, every
product/test/migration/locale/config/dependency/generated/original-dirty path,
legacy backend input, and `server/**` are read-only. Product behavior and bytes
must stay identical to exact S07D closure `8150a0e5` and r045 candidate
`a63bf58a`.

## Corrected lineage and external-artifact disposition

The complete Release 3 audit must validate raw commit-message bytes and parsed
trailers against each commit's registered authority:

- `71afb3c33a4d82431a8016cb195a97e5a36d8646` has exactly its frozen
  continuous ordered eight-field block and no `Issue` line;
- `9fb86ea056d253c82ea61b13031550db82eeb526` has exactly its nine
  registered ordered field lines with exactly one blank line between adjacent
  fields; all names and values must match v27/r032 even though standard Git
  parsing recognizes only its final field; and
- every other Release 3/r044/r045/r046 commit has exactly one complete,
  continuous, ordered nine-field block that standard Git trailer parsing
  returns in full.

No other missing field, blank-line split, duplicate, discontinuity, order drift,
value drift, or unregistered exception is accepted.

v39/v40 artifact deletion remains truthfully NON-PASS. For r046 only, host
policy retention is an acceptable terminal external-artifact disposition when
all of the following are freshly proven and independently reviewed:

- the exact task-owned F-drive path and recursive file/directory/byte inventory
  are recorded, remain outside both repository worktrees, and overlap no
  candidate path;
- every task-owned browser, process, and listener is closed, including ports
  3021, 38142, and 39142;
- the two rejected removal attempts and the fact that deletion never executed
  remain explicit; no retry, alternate deletion mechanism, or claim that the
  directory was removed is permitted; and
- fresh independent SPEC and CODE/SECURITY/QUALITY review explicitly accepts
  that disclosed retained state under this successor rule.

This disposition closes operational ownership without relabeling removal as a
PASS and does not establish a general cleanup waiver for another task.

## Ordered execution

1. R46.1 — Preserve r045 at exact blocked candidate `a63bf58a`; create
   `codex/release3-donegate-r046`, freeze this plan, mark r045
   `review-blocked-before-closure`, and activate only r046 with one complete
   continuous ordered nine-field trailer block.
2. R46.2 — Run the raw-message-aware full lineage audit and exact candidate,
   plan/policy hash, story tuple, path, product-drift, dirty-tree, process/port,
   and retained-artifact inventory audits. Record the result in only the four
   mutable governance records with r046 trailers.
3. R46.3 — Freeze one exact documentation-only r046 candidate and obtain fresh
   independent `SPEC PASS` and `CODE/SECURITY/QUALITY PASS` over the complete
   aggregate evidence, corrected lineage rule, and retained-artifact
   disposition.
4. R46.4 — Only after both fresh PASS decisions, record the exact candidate in
   the four mutable governance records, mark r046 complete, mark Release 3
   complete with zero active tasks, and leave Release 4 inactive.

## Acceptance and stop conditions

- The three-shape lineage rule above passes every commit from Release 3 base
  through the exact r046 candidate, using both raw-message and standard Git
  trailer parsing; a name/value match without the required shape is not PASS.
- r046 changes exactly the five authorized roadmap documents and zero product
  bytes. The complete Release 3 range remains free of `server/**`, generated,
  and original-dirty paths; both worktrees and all retained artifacts remain
  isolated.
- The exact R45.3 deterministic and R45.4 installed results remain applicable
  only if S07D product tree, the 114-path product patch, capability matrix, and
  all runtime-facing bytes are unchanged. Retained broad Views lint/root test
  NON-PASS and installed harness diagnostics remain visible.
- The external path remains retained only under the exact bounded disposition
  above. Any live task process/listener, repository overlap, unrecorded state,
  deletion claim, or alternate cleanup attempt blocks r046.
- Fresh independent `SPEC PASS` and `CODE/SECURITY/QUALITY PASS` are mandatory.
  Another BLOCK stops r046 and cannot be replaced by a primary-agent decision.

Release 4/S08+, S10 expansion, generated protobufs, external services, original
dirty paths, push, merge, deployment, legacy backend writes, and `server/**`
remain inactive or excluded.
